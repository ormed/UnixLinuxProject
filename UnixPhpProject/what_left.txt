QA:

Perl Commands:

1) add copy to manu. --------added------------
2) check rm and cp on hidden files. ------------checked and work------------
3) add mv command


Admin Commands:

1) adduser- fix the group.
2) Check all the fields in adduser/edituser (for example- empty fields).
3) Check what happens when changing user name or creating a new user with already exist username.
4) userdel - show list of all users?
5) backup/restore - add option to restore files in specific place.


File Browser:

1) connect copy and cut to interface.

File editor:

1) finish SED commands. ------------Done------------
2) add word count ?